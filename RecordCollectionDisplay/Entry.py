import MainWindow

class Entry:
    def __init__(self, artist, title, image, folder):
        self.artist = artist
        self.title = title
        self.imageURL = image
        self.genre = folder
